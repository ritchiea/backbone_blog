/ admin panel interface

%div#admin
  %div.title
    %h1
      Posts
    #old-posts
    %div.new-post-input
      %h2
        New Post
      %div#new-post
        %input#title{"placeholder" => "What's on your mind?", "type" => "text", :name => "title"}/
        %br/
        %textarea#text{"placeholder" => "Post body here"}
        %br/
        %input#save-post{:type => "button", :value => "Save"}


/ Templates

%script#post-template{"type" => "text/template"}
  <p class="post"><%= title %> <span class="edit">edit</span> <span class="<%= published ? 'publish' : 'unpublish' %>"><%= published ? 'publish' : 'unpublish' %></span></p>
%script#library-template{"type" => "text/template"}
  <ol class="posts">
  <% _.each(posts, function(post) { %>
  <li><%= post.title %></li>
  <% }); %>
  </ol>